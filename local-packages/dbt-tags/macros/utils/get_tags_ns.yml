macros:
  - name: get_tags_ns
    description: |
      Get the full qualified name (FQN) of the schema where tags are persisted to.

      **Usage**:
      ```sql
      {% raw %}
      {% set ns = dbt_tags.get_tags_ns() %}
      {% endraw %}
      ```